# client-demo

Vite + React demo app.

## Run with Docker (Alloy)

```bash
docker compose -f docker-compose.alloy.yaml up
```

The dev server listens on http://localhost:5173. In Alloy sessions the preview
is proxied at http://localhost:8080.

## Run locally

```bash
npm install
npm run dev
```
